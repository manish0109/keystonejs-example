extends ../layouts/default

block content
    script(src!='https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false')
    script.
        var map;
        function initialize() {
            var mapOptions = {
            center: new google.maps.LatLng(-34.859031,138.583061),
            zoom: 14
        };
        map = new google.maps.Map(document.getElementById("map-canvas"),mapOptions);
        }
        google.maps.event.addDomListener(window, 'load', initialize);

    .container
        .left-bar
            img(src='/images/left.jpg' alt='shopping photo')
        .right-bar
            #map-canvas
            p Shopping Centre
            p 400 Churchill Road, Kilburn, SA 5084
            p Phone 1234 5678
            
        